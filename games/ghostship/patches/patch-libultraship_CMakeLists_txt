https://github.com/Kenix3/libultraship/pull/971

Index: libultraship/CMakeLists.txt
--- libultraship/CMakeLists.txt.orig
+++ libultraship/CMakeLists.txt
@@ -50,6 +50,10 @@ if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
     include(cmake/dependencies/linux.cmake)
 endif()
 
+if(CMAKE_SYSTEM_NAME STREQUAL "OpenBSD")
+    include(cmake/dependencies/openbsd.cmake)
+endif()
+
 if (CMAKE_SYSTEM_NAME STREQUAL "Windows")
     include(cmake/dependencies/windows.cmake)
 endif()
