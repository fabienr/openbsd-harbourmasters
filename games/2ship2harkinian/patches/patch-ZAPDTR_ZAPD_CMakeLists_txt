Fix compilation for OpenBSD
https://github.com/HarbourMasters/ZAPDTR/pull/18

Index: ZAPDTR/ZAPD/CMakeLists.txt
--- ZAPDTR/ZAPD/CMakeLists.txt.orig
+++ ZAPDTR/ZAPD/CMakeLists.txt
@@ -480,12 +480,14 @@ elseif(CMAKE_SYSTEM_NAME STREQUAL "CafeOS")
 else()
 	set(THREADS_PREFER_PTHREAD_FLAG ON)
 	find_package(Threads REQUIRED)
+	find_package(Backtrace REQUIRED)
 	set(ADDITIONAL_LIBRARY_DEPENDENCIES
 		-Wl,--whole-archive $<TARGET_LINKER_FILE_DIR:OTRExporter>/$<TARGET_LINKER_FILE_NAME:OTRExporter> -Wl,--no-whole-archive
 		"libultraship;"
 		PNG::PNG
 		${CMAKE_DL_LIBS}
 		Threads::Threads
+		${Backtrace_LIBRARY}
 	)
 endif()
 
